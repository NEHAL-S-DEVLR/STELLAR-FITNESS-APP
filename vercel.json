{
  "buildCommand": "npm install && npm run build && cd backend && npm install",
  "functions": {
    "server.js": {
      "maxDuration": 15,
      "memory": 512,
      "includeFiles": "out/**"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/server.js" }
  ]
}
